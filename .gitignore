# Walled client vaults — confidential, NEVER committed.
# git push is a leak path that OS perms don't cover. Locked rule:
# vault/ is local-only. See STATE.md → Memory architecture → Vault rule.
vault/

# Secrets — shared-key home both brains source; NEVER committed (clone-clean, MEM-23).
# Track .env.example instead. See skills/*/SKILL.md for which keys each skill reads.
.env
.env.*
!.env.example

# Private memory DATA repo (OSS-1, Option D 2026-06-23). memory/ is its OWN git repo holding
# ONLY data (identity/logs/staging/sources/knowledge) — NEVER in the public cockpit history.
# Public = the system (memory-engine/ + memory-engine/DESIGN.md + skills); private = the data.
# memory-engine/bootstrap.mjs recreates this tree on a clone, so the public repo stays data-free.
memory/

# Runtime artifacts — private meeting audio/notes/state generated by shared skills.
artifacts/

# Engine runtime — deps pinned in package.json, never committed (the HF model cache lives in
# ~/.cache/huggingface, outside the repo).
memory-engine/node_modules/

# Personal roadmap + session narrative (OSS-1 option A — keep private).
# Public = the system; private = the data AND the personal strategy.
# Cloners get decisions/ (the reasoning) and the code; not the live roadmap or session log.
STATE.md
log/

# Local overrides — IDE, project-specific settings, etc.
*.local

# OS cruft
.DS_Store
